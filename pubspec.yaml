name: ios
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_hooks: ^0.21.1-pre.4
  shared_preferences: ^2.3.2
  json: ^0.20.2
  data_class: ^0.1.0
  collection: ^1.18.0
  localstore: ^1.4.0
  uuid: ^4.5.1
  flutter_local_notifications: ^18.0.0-dev.1
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_gpu:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/grammar.csv
